"use strict";
function sum(num1, num2) {
    return num1 + num2;
}
const result = sum(45, 55);
// If there is no return value
function consoleSomething() {
    console.log(3424);
}
const some = consoleSomething();
// Array Functions
// let multiply: (num1: number, num2: number) => number;
// multiply = (num1, num2) => num1 * num2;
// let multiply = (num1: number, num2: number): number => num1 * num2;
