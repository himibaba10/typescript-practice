function sum(num1: number, num2: number): number {
  return num1 + num2;
}

const result: number = sum(45, 55);

// If there is no return value

function consoleSomething(): void {
  console.log(3424);
}

const some: void = consoleSomething();

// Array Functions

// let multiply: (num1: number, num2: number) => number;
// multiply = (num1, num2) => num1 * num2;

// let multiply = (num1: number, num2: number): number => num1 * num2;
